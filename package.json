{
  "name": "my-media-manager",
  "version": "1.0.0",
  "description": "Multi-page Facebook media manager — content generation, scheduling, and analytics for AdSpot and other pages.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node server/index.js",
    "build": "npm --prefix client install && npm --prefix client run build",
    "postinstall": "npm --prefix client install || true"
  },
  "dependencies": {
    "@napi-rs/canvas": "^1.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-cron": "^3.0.3",
    "pg": "^8.12.0"
  }
}
